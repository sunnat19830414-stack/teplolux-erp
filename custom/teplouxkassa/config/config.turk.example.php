<?php
/**
 * Конфигурация направления "Турк" для мини-кассы Теплолюкс.
 * НЕ смешивать с конфигом Жоми — это единственное место, где прописаны границы изоляции направления.
 */
return [
    'direction_code'   => 'turk',
    'direction_label'  => 'Турк',

    // Логин в само мини-приложение (не Dolibarr!)
    'app_login'        => 'turk',
    'app_password'     => 'ЗАМЕНИТЬ_ПАРОЛЬ',

    // Dolibarr REST API
    'api_base_url'     => 'http://localhost/api/index.php',
    'api_key'          => 'ЗАМЕНИТЬ_API_КЛЮЧ',

    // Границы изоляции: только эти склады и только эти клиенты/товары (по префиксу ref)
    'ref_prefix'       => 'T',
    'warehouse_ids'    => [5, 6, 7],
    'default_warehouse_id' => 6, // 06 Турк склад Т
    'warehouse_labels' => [
        5 => '05 Жоме склад Т (Турк)',
        6 => '06 Турк склад Т',
        7 => '07 Турк склад Х',
    ],

    // Кассовые/банковские счета направления (id в Dolibarr llx_bank_account)
    // id = llx_bank_account.rowid, paytype = llx_c_paiement.rowid (4=Наличные/LIQ, 6=Карта/CB, 2=Перевод/VIR)
    // currency: 'USD' — кассир вводит сумму сразу в долларах (наличные); 'UZS' — кассир вводит сумму
    // в сумах + курс на момент операции (карта/QR/перевод физически идут в сумах, курс каждый раз свой,
    // не берётся из настроек — см. CLAUDE.md "Учёт в разных валютах" 28.08.2026).
    // Карта/QR/Перевод намеренно указывают на ОДИН и тот же служебный счёт (id=5, "Электронные платежи
    // Турк") — см. пояснение в config.zhomi.php. Раньше было 3 отдельных счёта — объединены 28.08.2026.
    'payment_accounts' => [
        'cash'     => ['id' => 4, 'label' => 'Наличные', 'paytype' => 4, 'currency' => 'USD', 'code' => 'LIQ'],
        'card'     => ['id' => 5, 'label' => 'Карта', 'paytype' => 6, 'currency' => 'UZS', 'code' => 'CB'],
        'qr'       => ['id' => 5, 'label' => 'QR', 'paytype' => 2, 'currency' => 'UZS', 'code' => 'VIR'],
        'transfer' => ['id' => 5, 'label' => 'Банк.перевод', 'paytype' => 2, 'currency' => 'UZS', 'code' => 'VIR'],
    ],

    'currency' => 'USD',
    'vat_rate' => 0, // без НДС (чёрная бухгалтерия) — цена товара = цена к оплате, без наценки

    // Кому физически передаётся наличная касса (для формы "Передать кассу" в разделе Касса/Долги) —
    // это теперь настоящий перевод: деньги реально списываются со счёта MuhammadAli и приходят на счёт
    // Sunnatilla (id=10, "Касса Sunnatilla"), а не просто обнуляются.
    'cash_recipient_label' => 'Sunnatilla',
    'cash_recipient_account_id' => 10,
    // Кто оформляет и утверждает заказы поставщику для этого направления (для экрана "Приём товара")
    'purchaser_label' => 'Абдурашид',

    // Бренды/категории направления (id => название), для плиток-навигации по товарам
    'categories' => [
        20 => 'NOBILI',
        30 => 'UNMAK БОЙЛЕР',
        38 => 'Botti',
        39 => 'Ascelik',
        40 => 'Ostendorf',
        41 => 'Icma',
        42 => 'EVS',
        43 => 'Hidronix',
        44 => 'Vana',
        45 => 'Candan',
        46 => 'Sanica',
        47 => 'E.C.A',
        48 => 'ETS & IFA Metal',
    ],
];
